# 赛事二专项优化规划・MCP 哨兵防护网关

> 第五届中国研究生网络安全创新大赛・赛题二（大模型智能体安全）专项作战规划
>
> 
>
> 作品代号:**MCP-Sentinel「关哨」** —— 面向在线 LLM Agent 的提示注入与工具滥用
>
> 检测 → 研判 → 阻断 → 溯源 闭环防护网关。

------

## 一、作品定位

赛题二要求：针对具备 **工具调用、文件系统操作、网络访问、子 Agent 派发** 能力的

在线运行 LLM Agent 系统，设计并实现一套 **提示注入与工具滥用的检测、研判、阻断、溯源闭环防护系统**。

- 系统形态

  :MCP-in-the-middle 中间代理 (主形态)+ 宿主 Agent Hook 插件 (辅形态)。

  

  即一个透明拦截在「LLM Agent ↔ 工具 / MCP server / 子 Agent」之间的安全代理节点，

  

  所有工具调用、资源读取、子 Agent 派发都流经它。

- **闭环四环节**: 检测 (识别注入)→ 研判 (意图偏离分析)→ 阻断 (三态决策)→ 溯源 (全链路 DAG)。

- 五大核心能力

  (逐条对应赛题): 多源注入识别 / 意图 - 计划 - 工具语义对齐 /

  

  细粒度工具策略与三态决策 / 跨 MCP 与子 Agent 溯源 / 对抗鲁棒性。

### 作品交付物总览 (命名均围绕赛题场景)

| 交付物            | 文件 / 模块代号   | 对应能力 |
| ----------------- | ----------------- | -------- |
| MCP 哨兵代理核心  | `sentinel_proxy`  | 系统形态 |
| 多源注入识别引擎  | `injection_radar` | 能力 1   |
| 意图对齐研判器    | `intent_aligner`  | 能力 2   |
| 语义策略 DSL 引擎 | `policy_dsl`      | 能力 3   |
| 跨链路溯源器      | `trace_dag`       | 能力 4   |
| 抗绕过加固层      | `evasion_shield`  | 能力 5   |
| 攻防靶场与用例集  | `arena`           | 验证     |

------

## 二、整体架构

text





```text
                ┌────────────────────────────────────────────┐
   用户输入 ───▶│              主 LLM Agent                    │
                │   (工具调用 / 文件 / 网络 / 子 Agent 派发)   │
                └───────────────┬─────────────────────────────┘
                                │ 每一次 tool_call / resource_read / sub_dispatch
                                ▼
        ┌───────────────────────────────────────────────────────────┐
        │             MCP-Sentinel 中间代理 (sentinel_proxy)          │
        │                                                             │
        │  ① injection_radar   多源注入识别(直接/间接/记忆)          │
        │  ② intent_aligner    意图-计划-工具语义对齐研判             │
        │  ③ policy_dsl        语义策略 DSL → 三态决策(放行/询问/阻断)│
        │  ④ trace_dag         全链路溯源(token级来源 + DAG/时序图)   │
        │  ⑤ evasion_shield    抗绕过归一化(贯穿①②③前置)            │
        └───────────────┬───────────────────────────────────────────┘
            放行│询问│阻断 ▼
                ┌────────────────────────────────────────────┐
                │   工具 / MCP server / 子 Agent (受控执行)    │
                └────────────────────────────────────────────┘
```

------

## 三、现有内核复用基线 (规划起点)

赛事二并非从零起步，以下现有模块作为复用底座 (本规划在其上做专属升级):

| 现有模块                                                     | 复用为         | 升级方向                                                  |
| ------------------------------------------------------------ | -------------- | --------------------------------------------------------- |
| `security/policy/engine.py`(JSON 可配策略：SQL / 文件 / API / 邮件 / 频率) | 策略 DSL 雏形  | 升级为语义级 DSL, 补 fs/net/exec/git/ 子 Agent 维度与三态 |
| `security/tool_guard.py` / `goal_guard.py` / `memory_guard.py`(allow/block + attribution) | 检测与归因地基 | 二态→三态，归因升级为 token 级来源                        |
| `security/audit.py`(哈希链 + Ed25519 + trace_id + tool_call_id) | 溯源数据底座   | 在 trace_id 上构建 DAG 还原                               |
| `auto_attack_system/indirect_injection.py`(HTML/Email/MD 投毒) | 间接注入语料   | 补 URL / 工单 / Issue/MCP 字段 / 工具描述 等来源          |
| `auto_attack_system/payloads/obfuscation.py`                 | 抗绕过基础     | 系统化为绕过手法矩阵                                      |
| `detection/goal_drift`                                       | 意图偏离基础   | 升级为意图 - 计划 - 工具三元对齐                          |

------

## 四、任务包拆解 (按逻辑先后排布，无时间节点)

> 每个任务包 (Task Package, TP) 含：任务边界 / 关键产出 / 交付标准。
>
> 完成最后一个任务包 (TP-09) 即代表整个赛事二作品落地完成。

------

### TP-00・在线 Agent 靶场基座搭建 (arena-base)

**任务边界**

构建一个具备赛题要求四种能力的、可在线运行的 LLM Agent 靶场，作为被防护对象。

该 Agent 须真实具备：工具调用、文件系统读写、网络访问 (net.fetch)、子 Agent 派发。

不复用电商业务设定，改为通用「研发助手 Agent」场景 (读代码 / 查文档 / 跑命令 / 调外部 API),

以贴合赛题示例 (读 README、读 SSH 私钥、git.push、子 Agent 传密钥等)。

**关键产出**

- `arena/dev_agent`: 主 Agent + 至少 6 个工具 (`fs.read`/`fs.write`/`net.fetch`/`exec`/`git.push`/`sub_agent.dispatch`)
- `arena/mcp_servers`:≥2 个模拟 MCP server (文件服务 / 网络服务), 供后续多 MCP 溯源
- `arena/sub_agents`:≥1 个后台子 Agent (模拟派发场景)
- 接 Qwen 真实模型运行 (保留离线确定性 fallback)

**交付标准**

- 单命令拉起靶场，主 Agent 能在无防护下真实完成正常任务 (读 README、查文档)
- 无防护下可被攻击诱导执行越权动作 (读私钥、外发数据), 作为后续防护的对照基线
- 工具调用、资源读取、子 Agent 派发三类事件均产生结构化事件流

------

### TP-01・MCP 哨兵代理骨架 (sentinel_proxy-core)

**任务边界**

实现 MCP-in-the-middle 透明代理：拦截 TP-00 靶场中 Agent 与工具 / MCP / 子 Agent 的全部交互，

形成统一的「拦截 → 流水线判定 → 三态放行」管道。本任务只搭管道与拦截点，不实现具体检测算法。

**关键产出**

- `sentinel_proxy/interceptor`: 统一拦截器，捕获 tool_call /resource_read/sub_dispatch 三类事件
- `sentinel_proxy/pipeline`: 可插拔判定流水线 (radar → aligner → policy → trace 钩子位)
- `sentinel_proxy/context`: 会话上下文聚合器 (原始用户意图、对话历史、已读资源、工具调用序列)
- Hook 插件适配层：同一判定核心可挂为宿主 Agent Hook (满足 "一种或多种组合")

**交付标准**

- 靶场所有工具调用 100% 流经代理，无绕过通道
- 代理可在「纯透传 (observe)」与「拦截 (enforce)」两种模式切换
- 每个被拦截事件携带完整上下文 (意图 / 历史 / 资源来源 / 调用参数), 供下游消费

------

### TP-02・抗绕过归一化前置层 (evasion_shield)

**任务边界**

实现赛题能力 5 要求的抗绕过能力，作为所有检测的**前置归一化层**(先归一化再检测，避免被变形绕过)。

覆盖赛题点名的全部七类绕过手法。本任务先做归一化与还原，绕过用例集放在 TP-08 验证。

**关键产出**

- ```
  evasion_shield/normalizer
  ```

  , 逐类处理:

  - 多语言混淆 (中 / 英 / 日 / 俄 + Emoji 混排)→ 统一语义抽取
  - Unicode 变形 (西里尔 / 拉丁同形替换、零宽字符、不可见字符)→ 同形归一 + 隐藏字符剥离
  - 分词拆分 (分片拼接、多轮拆分、空白间隔)→ 跨片 / 跨轮重组
  - 分隔符注释 (`/* */`、`#` 内嵌、`;`/`&&` 拼接)→ 注释剥离 + 命令拆解
  - 字符格式 (大小写混合、全角半角、特殊编码)→ 大小写 / 全半角归一 + 解码
  - 上下文稀释 (超长内容冲淡前置规则)→ 关键指令提取，不受长度影响
  - 语义伪装 (恶意指令包装成正常业务)→ 交由 TP-03 意图层兜底，本层标注可疑包装

**交付标准**

- 对每类绕过手法，归一化后能还原出底层真实指令
- 归一化层对正常良性输入零误伤 (不破坏正常语义)
- 输出归一化后的标准化文本 + 检测到的绕过手法标签，供 TP-04 检测消费

------

### TP-03・多源注入识别引擎 (injection_radar)

**任务边界**

实现赛题能力 1: 同时覆盖三类攻击来源的注入识别，接收 TP-02 归一化后的文本。

**关键产出**

- `injection_radar/direct`: 直接注入识别 (用户输入的越权指令、角色伪装、上下文劫持)

- ```
  injection_radar/indirect
  ```

  : 间接注入识别，覆盖赛题列举的全部外部资源来源:

  - URL 网页内容、文档解析结果、IDE 打开的源码文本
  - 工单 / 邮件 / Issue 内容、MCP 工具返回字段、工具注册描述信息

- `injection_radar/memory`: 记忆污染识别 (跨会话长期记忆库、向量库、业务知识库中预植入的隐性触发指令)

- 每条识别结果输出：命中来源类型、注入载荷定位、置信度

**交付标准**

- 三类来源 (直接 / 间接 / 记忆) 各自可独立识别并给出来源归属
- 间接注入覆盖赛题列举的全部 6 类外部资源来源
- 识别结果标注出「攻击载荷对应的 token 级文本位置」, 为 TP-06 溯源提供来源锚点

------

### TP-04・意图 - 计划 - 工具语义对齐研判器 (intent_aligner)

**任务边界**

实现赛题能力 2: 基于对话上下文与全量工具调用序列，刻画「用户原始任务意图」与

「Agent 实际执行计划」的语义偏离度，输出可解释研判结论。这是赛题 "研判" 环节的核心。

**关键产出**

- `intent_aligner/intent_model`: 从用户原始请求抽取任务意图 (借助 Qwen + 规则)
- `intent_aligner/plan_tracker`: 跟踪 Agent 实际执行计划 (工具调用序列 + 参数 + 资源访问)
- `intent_aligner/deviation`: 计算意图 - 计划偏离度，定位偏离发生的具体工具调用
- `intent_aligner/verdict`: 生成赛题要求的可落地、可解释研判结论文本

**交付标准**

- 能复现赛题示例级别的研判输出，例如:

  

  「用户原始请求为读取 README 文档，Agent 却调用 fs.read 读取本地 SSH 私钥，

  

  并通过 net.fetch 向外部未知域名发起数据传输，判定为间接提示注入引发的工具滥用」

- 输出包含：原始意图 / 实际计划 / 偏离点 / 攻击类型判定 / 可解释理由

- 对正常任务 (意图与计划一致) 判定为无偏离，不产生误判

------

### TP-05・语义策略 DSL 与三态决策引擎 (policy_dsl)

**任务边界**

实现赛题能力 3: 摒弃名称 / 路径黑白名单粗粒度匹配，设计工具调用级别的**语义策略 DSL**,

每条策略支持运行时风险等级评估，输出「放行 / 询问 / 阻断」三态决策。

在现有 `policy/engine.py` 基础上升级为语义级、可声明的 DSL。

**关键产出**

- `policy_dsl/schema`: 声明式策略 DSL 语法 (条件 = 工具 + 参数语义 + 上下文 + 数据流)
- `policy_dsl/engine`:DSL 解释执行 + 运行时风险分级 + 三态裁决
- 内置赛题点名的五类精细化策略规则:
  - 限制 `fs.write`: 禁止写入 `~/.ssh/`、`~/.aws/`、`/etc/cron.*`、系统自启动目录
  - 限制 `net.fetch`: 禁止将本会话内读取的本地敏感文件 / 密钥外发至非信任域名 (数据流追踪)
  - 限制 `exec`: 禁止 base64 解码后执行、远程下载管道执行等等价恶意命令
  - 限制 `git.push`: 用户任务为代码检视时禁止触发代码提交推送
  - 限制 子 Agent 派发：禁止向后台子 Agent 传递本会话内的密钥 / 凭证
- 三态裁决联动：高风险→阻断、中风险→询问用户确认、合规→放行

**交付标准**

- 五类策略规则全部可运行、可命中、可解释 (给出命中规则名 + 风险等级 + 理由)
- 策略基于「参数语义 + 数据流」而非纯字符串匹配 (如 net.fetch 能识别 "外发的是本会话读到的密钥")
- 三态决策真实生效：阻断拦截执行、询问产生确认交互、放行透传
- 策略以 DSL 声明，非硬编码，可增删改而不改引擎代码

------

### TP-06・跨 MCP 与子 Agent 溯源器 (trace_dag)

**任务边界**

实现赛题能力 4 (最大缺口): 针对多 MCP server、多层级子 Agent 协同场景，

完整还原单次违规操作的全链路调用关系，输出可视化攻击路径 DAG 与时序链路图。

在现有 `audit.py` 的 trace_id /tool_call_id/ 哈希链基础上构建。

**关键产出**

- `trace_dag/collector`: 贯穿全链路采集 (主 Agent → 子 Agent → 中转 MCP 代理节点)

- ```
  trace_dag/resolver
  ```

  , 还原赛题要求的五要素:

  - 攻击触发源 (用户输入 / 外部读取资源 / 记忆库污染内容)
  - 全链路节点 (经过的主 Agent、子 Agent、MCP 代理节点)
  - 上下文来源 (攻击载荷对应的 token 级文本来源与归属，接 TP-03 锚点)
  - 调用详情 (全量工具调用、传入参数、返回结果)
  - 影响范围 (被操作文件、外发网络目标、操作的 Git 仓库、访问的凭据库)

- `trace_dag/visualize`: 生成攻击路径有向无环图 (DAG)+ 时序链路图

**交付标准**

- 任一被阻断 / 告警事件均可一键还原完整调用链，五要素齐全
- 跨 ≥2 个 MCP server 与 ≥1 层子 Agent 的攻击能被完整串联
- 输出可视化 DAG 与时序图，支撑安全研判与复盘
- 攻击载荷可定位到 token 级来源 (与 TP-03 识别结果对应一致)

------

### TP-07・闭环联动整合 (sentinel-loop)

**任务边界**

把 TP-02~TP-06 五大能力串成赛题要求的「检测 → 研判 → 阻断 → 溯源」完整闭环，

挂载到 TP-01 代理流水线上，形成单一可运行的防护系统。本任务做编排与联动，不新增算法。

**关键产出**

- `sentinel_proxy/pipeline` 完整编排：归一化 → 多源识别 → 意图研判 → 策略三态 → 溯源记录
- 统一事件模型：一次工具调用从拦截到裁决到溯源全程串联同一 trace_id
- 实时监督输出：每次决策实时产出 (放行 / 询问 / 阻断 + 研判理由 + 溯源引用)
- 联动规则：识别到注入 → 触发意图研判 → 命中策略 → 三态裁决 → 写入溯源 DAG

**交付标准**

- 一次完整攻击从输入到被阻断到溯源，全链路一气呵成，无人工干预
- 检测、研判、阻断、溯源四环节输出在同一条记录中可追溯关联
- 系统可在 observe (只检测不拦)/enforce (检测 + 拦截) 两模式运行

------

### TP-08・攻防靶场用例集与对抗验证 (arena-eval)

**任务边界**

构建覆盖五大能力的攻击用例集，在 TP-07 闭环系统上做对抗验证，产出量化指标。

攻击用例复用并扩展现有 payloads/injectors, 聚焦赛题场景 (研发助手 Agent)。

**关键产出**

- `arena/cases/multi_source`: 直接 / 间接 / 记忆三类注入用例 (覆盖能力 1 的全部来源)

- `arena/cases/tool_abuse`: 五类工具滥用用例 (对应 TP-05 五条策略)

- `arena/cases/evasion`: 七类绕过手法用例 (对应能力 5, 验证 TP-02)

- `arena/cases/cross_chain`: 跨 MCP / 子 Agent 攻击用例 (验证 TP-06 溯源)

- 量化指标：注入识别召回率 / 精确率、意图偏离研判准确率、三态决策正确率、

  

  溯源完整率、抗绕过通过率、良性请求误伤率

**交付标准**

- 五大能力每项均有专属用例集，且能在闭环系统上跑通
- 关键指标量化达标：注入识别与工具滥用拦截高召回，良性请求低误伤
- 抗绕过用例验证七类手法均无法绕过防护
- 跨链路攻击用例可被完整溯源 (DAG 五要素齐全)

------

### TP-09・安全研判与复盘可视化 (sentinel-console)

**任务边界**

实现赛题要求的「可视化的攻击路径 DAG 与时序链路图」+ 监督端展示，作为作品最终落地形态。

完成本任务即代表整个赛事二作品落地完成。

**关键产出**

- `sentinel_console/dag_view`: 攻击路径有向无环图可视化 (节点 = Agent/MCP/ 工具，边 = 调用关系)
- `sentinel_console/timeline_view`: 时序链路图 (按时间还原攻击全过程)
- `sentinel_console/verdict_panel`: 研判结论展示 (意图偏离 + 攻击类型 + 影响范围 + 三态决策)
- 监督端实时展示：放行 / 询问 / 阻断决策流 + 告警 + 溯源入口
- 安全风险分析输出：面向赛题的攻击场景与防护效果说明 (随作品内嵌，非企业化交付)

**交付标准**

- 任一攻击事件可在控制台一键查看其 DAG + 时序图 + 研判结论 + 影响范围
- 监督端实时呈现三态决策与告警，支撑现场演示与复盘
- 五大能力在可视化层全部有对应呈现，作品形成完整闭环，落地完成

------

## 五、任务包依赖关系

text





```text
TP-00 靶场基座
   │
   ▼
TP-01 代理骨架 ──────────────┐
   │                         │
   ▼                         │
TP-02 抗绕过归一化(前置)     │
   │                         │
   ├──▶ TP-03 多源注入识别    │
   │         │               │
   │         ▼               │
   ├──▶ TP-04 意图对齐研判    │
   │         │               │
   ├──▶ TP-05 语义策略DSL三态 │
   │         │               │
   └──▶ TP-06 跨链路溯源DAG ◀─┘
             │
             ▼
        TP-07 闭环联动整合
             │
             ▼
        TP-08 对抗验证用例集
             │
             ▼
        TP-09 研判可视化(落地完成)
```

------

## 六、五大能力 ↔ 任务包覆盖核对表

| 赛题核心能力                      | 主责任务包 | 支撑任务包                                |
| --------------------------------- | ---------- | ----------------------------------------- |
| 能力 1 多源注入识别               | TP-03      | TP-02 (归一化)、TP-08 (用例)              |
| 能力 2 意图 - 计划 - 工具语义对齐 | TP-04      | TP-01 (上下文聚合)、TP-08                 |
| 能力 3 细粒度工具策略 + 三态决策  | TP-05      | TP-08                                     |
| 能力 4 跨 MCP / 子 Agent 溯源     | TP-06      | TP-00 (多 MCP / 子 Agent)、TP-09 (可视化) |
| 能力 5 对抗鲁棒性                 | TP-02      | TP-08 (绕过用例)                          |
| 系统形态 (MCP 中间代理)           | TP-01      | TP-07                                     |
| 闭环 (检测→研判→阻断→溯源)        | TP-07      | 全部                                      |