# MCP-Sentinel 路线图

本路线图围绕“生成式大语言模型与智能体”赛题组织：用大模型驱动的多智能体系统解决 LLM Agent 运行安全这一实际问题。路线图区分已经由代码和测试验证的能力，以及仍需补充的在线模型证据，避免把目标形态写成既成事实。

## 成功标准

1. 系统能把自然语言安全目标解析为结构化任务和执行计划。
2. Attack Agent 能根据历史失败调整攻击动作并扩展覆盖面。
3. Defense Agent 能根据损伤报告选择精确加固动作，而不是固定执行同一规则。
4. Evaluation Agent 能量化攻击成功率、覆盖率、误伤率与收敛状态，并反馈下一轮决策。
5. 工具执行、环境观察、重规划和防御决策均能进入 trace 与审计证据。
6. 在线模型轨迹与离线确定性复现明确区分，指标可从命令重新生成。

## 当前已实现

| 能力 | 实现 | 验证方式 |
|---|---|---|
| 任务解析与计划 | `task_agent` 使用结构化 LLM 输出生成 TaskSpec 和 Plan | `python run.py --agent-demo --offline` |
| 攻击规划与反思 | Attack Agent 根据失败历史选择候选载荷和升级策略 | `python run.py --attack-campaign --offline` |
| 环境感知 | 工具结果、库存、策略阻断、目标漂移和记忆污染进入 observation | 全量测试与闭环演示 |
| 防御自主决策 | Defense Agent 根据被攻破类别选择 prompt、rule、retrieval 或 rerank 加固 | `python run.py --defense-regression --offline` |
| 自动评测 | 计算 ASR、覆盖率、误伤率、任务达成和重规划指标 | `python run.py --evidence-pack --offline` |
| MCP 调用治理 | 统一拦截、注入识别、意图对齐、三态策略和 trace DAG | `arena`、`sentinel_proxy` 及相关测试 |
| 可审计证据 | JSON、Markdown、图表、时间线和哈希链 | `docs/competition/evidence-pack` |

## 提交前工作包

### WP-1：统一项目身份

- 项目、仓库、发行包和文档统一使用 MCP-Sentinel / `mcp-sentinel`。
- 清理旧品牌、旧仓库地址和其他赛题叙事。
- 统一 README、报告、讲稿、复现说明和证据数字。

### WP-2：强化大模型驱动证据

- 使用有效 Qwen 兼容 API 生成任务解析、攻击规划、防御决策和失败反思轨迹。
- 每条在线证据记录模型名、时间、实际运行模式和 fallback 状态。
- 在线 API 失败时明确标记 fallback，不把确定性结果标为在线模型输出。

### WP-3：扩展任务与场景评测

- 在现有攻击面与电商任务上补充多步骤、失败恢复、预算、缺货、越权和工具异常场景。
- 保存每次运行对应的环境状态，避免仅依赖 Agent 自报的目标达成结果。
- 分别报告在线模型和离线 fallback 指标。

### WP-4：提交与答辩

- 固定一条八分钟主线：目标解析 → 攻击探索 → 环境反馈 → 防御决策 → 再评测 → 收敛证据。
- 保留自然语言电商任务演示作为通用 Agent 能力补充。
- 提交包排除运行产物、缓存、密钥和本地存储。

## 非目标

- 不连接真实支付、真实用户数据或真实外部攻击目标。
- 不在本阶段迁移所有内部模块到单一 Python 命名空间。
- 不把本地模拟指标外推为生产环境安全效果。
